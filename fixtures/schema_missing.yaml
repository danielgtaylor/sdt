document:
  schemas:
    input:
      type: object
      properties:
        foo:
          type: string
  template:
    value: ${foo}
tests:
  - input:
      foo: some test value
    expected:
      value: some test value
