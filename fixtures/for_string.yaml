document:
  schemas:
    input: {}
    output:
      type: string
  template:
    $for: items
tests:
  - input: {}
    errors:
      - $for expression must use ${...} interpolation syntax
    expected: {}
