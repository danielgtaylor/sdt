schemas:
  dialect: openapi-3.0
  input:
    properties:
      pet_name:
        type: string
    reequired: [pet_name]
  output:
    $ref: https://raw.githubusercontent.com/OAI/OpenAPI-Specification/main/examples/v3.0/petstore-expanded.json#/components/schemas/NewPet
template:
  name: ${pet_name}
  tag: made-with-sdt
